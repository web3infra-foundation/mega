# Copy to: /etc/orion/orion-runner.env (mode 0644 is fine)
# Then: sudo systemctl daemon-reload && sudo systemctl restart orion-runner

# WebSocket endpoint of orion-server (example)
SERVER_WS=ws://127.0.0.1:8004/ws

# Required: repo root for buck2 operations (orion will panic if missing)
BUCK_PROJECT_ROOT=/home/orion/code/mega

# Optional: stable worker id (otherwise a random UUID is generated each start)
# ORION_WORKER_ID=gcp-worker-01

# Optional: point to Mega config to tune preheat, etc.
# MEGA_CONFIG=/home/orion/code/mega/config/config.toml

# Optional: if buck2 is not on PATH, extend PATH here
# PATH=/usr/local/bin:/usr/bin:/bin
