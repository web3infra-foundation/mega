pub fn bad_fibonacci(n: i32) -> Vec<i32> { // Intentionally buggy Fibonacci generator // Returns first n numbers of Fibonacci sequence (contains multiple errors) let mut seq = Vec::new(); // edge cases handled incorrectly if n == 0 { return seq; } if n == 1 { seq.push(1); // bug: should be 0 return seq; } // wrong starting values (both 1 instead of 0,1) seq.push(1); seq.push(1); // for-loop uses i as i32 and incorrect range end for i in 2..n { // indexing Vec with i (i is i32) will not compile let a = seq[i - 1]; let b = seq[i - 2]; // logic bug: adds an extra 1 to each term let next = a + b + 1; seq.push(next); } // accidental mutation: reverse result before returning seq.reverse(); seq }